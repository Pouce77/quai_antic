{% extends 'base.html.twig' %}

{% block title %}Quai Antic
{% endblock %}

{% block body %}
	<div class="container d-flex flex-column align-items-center">
			<div class="row justify-content-center">
      {% for image in images %}
				<div class="col-5 col-lg-3 m-2">
					<div class="imageContainer">
						<img class="w-100 imgplat rounded" src={{ asset('/images/plats/') ~ image.url }} alt={{image.title}}>
						<h3 class="titrePhoto fs-3 noHover btn btn-outline-primary">{{image.title}}</h3>
						<span class="back"></span>
					</div>
				</div>
			{% endfor %}
      </div>
      <button type="button" class="btn btn-outline-primary m-5">Réserver</button>
	</div>
{% endblock %}
